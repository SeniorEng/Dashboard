- [test-utils path needs /api prefix](test-utils-path-no-api-prefix.md) — apiGet/apiPost/apiDelete(+*As) build `${BASE_URL}${path}` with NO /api prefix; integration test paths must start with `/api/...` or you get a misleading 404.
- [matchRows duplicate-match test trap](import-matchrows-dup-key.md) — import test getting `updated:0`/`billedProtected undefined`? Seeded appt's START time must equal the fixture's or matchRows never matches it; force the diff on duration/end, not start.
- [Ephemeral orchestrator output capture](ephemeral-orchestrator-output-capture.md) — backgrounded `with-ephemeral-db.ts` runs always log 0 bytes (every redirect/PTY variant); only platform workflow PTY captures it; `pkill -f with-ephemeral-db` kills your own shell — kill by PID.
- [Lexware hours gated documented-only](lexware-export-documented-only-gate.md) — lexware-export hours/travel/km use documentedSqlRaw (status=completed, decoupled from signature); tests/lexware-export-signed-gate.test.ts asserts old signed-gating ⇒ stale & red on HEAD; don't re-add documentedAndSignedSqlRaw to "fix" it.
- [Lexware Urlaub/Krankheit = day-count only](lexware-urlaub-krankheit-day-count.md) — minijobber vacation/sick reported as DAYS not bruttoCents on purpose; Lexware computes Entgeltfortzahlung itself (back-office confirmed); don't "fix" by adding pay.
- [Billing breakdown hours SSoT (0,0h fix)](billing-breakdown-hours-ssot.md) — /admin/billing per-period hours must read invoice_line_items.duration_minutes for late stages (invoiced appts leave appt-stages → 0,0h); mirror pipeline reader's hybrid breakpoint exactly ⇒ Σ rows === stageTotalCents by construction.
- [§45b forecast prod vs deployment lag](45b-forecast-prod-deployment-lag.md) — a negative §45b "Verfügbar nach Planung" on LIVE while current code + prod data are clean = OLD asymmetric forecast (already fixed via netAvailable45bAt SSoT) running on a stale build ⇒ re-publish, not a new fix; reproduce real prod data vs current readers before chasing code.
- [Atomic customer create](atomic-customer-create.md) — one wizard submit + one DB tx (no orphan customer, no secondary POSTs); PDF/object-storage writes inside tx but NOT rolled back; carryover is hard, `warnings` serves only contacts; SetupPendingBanner kept, intake-checklist removed.
- [Month-close 8th-only unconditional close](month-close-unified-readiness.md) — Task #1496: 8th auto-close is the ONLY mechanism (no manual/batch/reopen), UNCONDITIONAL, never overwrites status (expired_unsigned is display-derived); missing sig = follow-up not blocker; "documented"==completed decoupled from "signed"; activity is the only gate; doc-via-admin sets performed_by=admin.
- [Ephemeral test-DB parallelism & PID ceiling](ephemeral-test-db-pid-ceiling.md) — cap parallel test workers low (pids.max=1024); EAGAIN mass-fail = resource exhaustion not logic; fresh DB breaks tests that GET list→data[0]; workflow-log capture is stale, verify via synchronous bash runs.
- [Ephemeral test-DB fail-fast guard](ephemeral-db-test-guard.md) — integration globalSetup aborts bare `vitest run` against a non-throwaway DB; allowed only via CI=true, orchestrator var, or cc_test_ db-name; never guard the shared unit setup (blocks pure unit runs).
- [CSRF auto-heal in API client](csrf-auto-heal-client.md) — strict CSRF cookie vs lax session can 403 a sign POST; apiRequest force-refreshes token + retries once on CSRF_TOKEN_*; new write paths (postFormData) must inherit; prod text/html 403 = edge throttle, not app CSRF.
- [Edge WAF blocks data: URIs](edge-waf-data-uri-block.md) — published edge 403s any request body containing `data:image/...;base64,` (size irrelevant); signatures strip prefix client-side, server restores. New image-in-JSON write paths must go through apiRequest.
- [GitHub-Sync automation](github-sync-automation.md) — Replit→GitHub main sync MUST be Replit-side (scripts/github-sync.sh + Scheduled Deployment); GitHub Actions can't pull Replit; push falls back connector-token→GITHUB_WORKFLOW_PAT on GH013/missing.
- [Lockfile firewall-URL normalization](lockfile-firewall-normalization.md) — committed package-lock must use registry.npmjs.org not package-firewall; .npmrc/replace-registry-host CAN'T fix it (env var + firewall packument), only the postinstall normalize-lockfile.mjs hook; never re-add the per-step CI sed.
- [GitHub Actions CI live + re-publish trap](ci-workflow-not-on-github.md) — ci.yml runs on SeniorEng/Dashboard; only GITHUB_WORKFLOW_PAT can edit `.github/workflows/`; GitHub-direct ci.yml fixes regress on re-publish unless mirrored into the isolated env; lockfile firewall-URL normalization now via postinstall hook (see lockfile-firewall-normalization.md, no more per-step sed), veraPDF find needs -mindepth 1.
- [CI Neon-proxy fixed-target bridge](ci-neon-proxy-fixed-target.md) — proxy ignores db name in DATABASE_URL → app-server/seeds can't route per-DB in CI; use EPHEMERAL_PROVISION_ONLY=1 + direct psql/drizzle-kit to verify ephemeral-DB/cache machinery.
- [Ephemeral test DB seed contract](ephemeral-test-db-seed-contract.md) — per-run throwaway DB has ONLY seeded superadmin + base services + company_settings; tests must self-seed extras and derive all date anchors from ONE computed anchor (month-boundary trap).
- [storage.getInvoice JOIN-alias trap](storage-get-invoice-customername.md) — getInvoice OVERRIDES invoice.customerName with customers.name; any verifier/re-render that selects raw from invoicesTable will drift.
- [Loaded invoices use frozen snapshot name](invoice-frozen-snapshot-name.md) — getInvoice/getInvoices return snapshot?.customer?.name ?? live; verifier/re-render build from snapshot; reading live name re-introduces hash drift. (opposite pull from storage-get-invoice-customername.)
- [Pflegekasse billing needs customer sig](pflegekasse-billing-customer-sig-gate.md) — billingType-aware gate: Pflegekasse(gesetzlich/privat) bills only on status=completed; employee_signed alone → 400; Selbstzahler still bills on employee_signed. All tests/billing/* must dual-sign.
- [Audit-refresh banner trap](audit-refresh-banner-trap.md) — chunked full-app audit refreshes may banner old content instead of re-walking; verify chunk header commit hash before trusting severity counts.
- [Budget Cap-SSoT (computeCapSlot)](budget-cap-ssot-computecapslot.md) — §45a/§39_42a Anzeige UND Buchung müssen über computeCapSlot rechnen; eigene allocated−used-Mathe driftet, sobald monthlyLimit > statutorischer Cap.
- [§45a statutory default cap](45a-statutory-default-cap.md) — PG≥2 + §45a enabled + monthlyLimitCents null ⇒ resolve45aMonthlyLimitCents default; MUST flow through BOTH cap-math AND getCustomerBudgetAmounts or overview shows 0 / drifts. Explicit (incl. 0) overrides.
- [Budget statutory input caps](budget-statutory-input-caps.md) — §45a/§39-§42a caps on PUT/create/wizard, per-pot enabled-gated (not value>0); clampToStatutoryMax=SSoT; over-cap test fixtures must storage-seed.
- [Cost-Estimate Klassifikation](budget-cost-estimate-classification.md) — vier Branches (Selbstzahler/OK/Soft-Private/Hard-Block) leben als pure classifyCostEstimate() in shared/domain/budget/; Route ist nur Wrapper.
- [Budget write_off-Asymmetrie](budget-write-off-asymmetry.md) — write_off zählt in Topf-/Allocation-Sicht als Used, NICHT in Fenster-Cap-Sicht; jede neue budget_transactions-Aggregation muss sich aktiv entscheiden.
- [Reversal rows keep appointmentId](budget-reversal-keeps-appointmentid.md) — storno/reversal behält jetzt orig.appointmentId (GoBD CHECK); „live" Consumption = type='consumption' minus Rows, auf die ein reversedTransactionId zeigt.
- [audit_log immutability](audit-log-immutability-triggers.md) — append-only via raising BEFORE triggers (silent RULEs/REVOKE taugen nicht, App ist DB-Owner); legitimes Mutieren nur via `SET LOCAL app.allow_audit_log_mutation='on'`; Drizzle-Fehler zeigt nur „Failed query", nicht die RAISE-Message.
- [GoBD table immutability triggers](gobd-table-immutability-triggers.md) — budget_allocations/cbts/invoices/invoice_line_items per BEFORE-Trigger geschützt; Bypass `SET LOCAL app.allow_gobd_mutation='on'`; Cascade-Deletes über Kunden zwingen Bypass in jeden customer-purge.
- [Budget settings read mode (forDate vs forEdit)](budget-settings-read-mode-fordate.md) — booking/availability/reservation/rebook-exec read type-settings forDate@txDate; forEdit/withTransition only for settings-edit + rebook-preview (exec reuses preview ⇒ no drift); arch-test guarded.
- [Budget-Type-Settings Window-Shift-Falle](budget-typesettings-window-trap.md) — allocStart/end aus FRÜHESTEM validFrom / SPÄTESTEM validTo aller Zeilen ableiten, nicht aus einer einzelnen typeSettings.find()-Row, sonst kippt jede append-only-Transition.
- [Budget-Type-Settings Phasen-Append](budget-typesettings-phase-append.md) — zukünftiges validFrom MUSS Phasen-Append (Vorgänger schließen, einklemmen) triggern, NICHT den isStillFresh-In-Place-Pfad; sonst überschreibt jeder Folge-PUT die Vorgänger-Phase.
- [Budget-Architektur-Doku ausgelagert](budget-doc-layout.md) — Budget-Details NICHT in replit.md ablegen; docs/architecture/budget.md ist die Quelle, replit.md hält nur Pointer.
- [Invoice-per-pot split (Variant C)](invoice-per-pot-split.md) — Multi-Pot-Lauf → N Rechnungen via billing_run_id; Σ-Invariante via shared/domain/budget-invoice-split.ts (Largest-Remainder), Cascade-Storno explizit per Flag.
- [Billing Bündel-Druck vs Sammeldruck](billing-bundle-vs-sammeldruck.md) — /:id/bundle = per-invoice read-only PDF; /bulk-print = month-wide merged PDF/ZIP that also marks "versendet". Don't consolidate.
- [Selbstzahler/private = uncapped pot](budget-selbstzahler-uncapped-pot.md) — alle Kunden über EIN planCascade; private=uncapped Terminal-Topf (capacity nie als Zahl lesen), kein Rest⇒BudgetHardBlockError; Equality-Test bucht km=0 ⇒ km-quantize-Drift unsichtbar.
- [§45b onboarding baseline](45b-onboarding-baseline.md) — Vorjahr gilt als aufgebraucht; §45b-Runtime-Pfad bodet den aus Pflegegrad-Historie abgeleiteten Anker aufs laufende Jahr (floorAutoAnchor45bToCurrentYear); clampDerived/earliest45bRelevantAnchor nur noch unit-getestete Helfer.
- [Stryker mutation setup](stryker-vitest4-command-runner.md) — vitest4 hangs the stryker vitest-runner → use command runner; mutate ONLY pure shared/domain calc modules; allowlist ignorePatterns to dodge EISDIR sandbox-copy crash.
- [vi.spyOn(console) broken](vitest-spyon-console-broken.md) — vi.spyOn(console,"log") does NOT capture console.log from imported modules in this vitest setup; rebind `console.log` directly and restore in finally.
- [Test signature fixtures min bytes](test-signature-fixture-min-bytes.md) — 8-byte PNG stub fails signature-validation (Task #749) → setup EMPTY_SIGNATURE; use tests/helpers/signature.ts VALID_SIGNATURE_DATA_URL.
- [focus() on disabled button is no-op](focus-disabled-button-race.md) — dialog auto-focus useEffect must depend on/guard the is-pending flag, else focus never lands → Radix Escape flakes.
- [Appointment→employee attribution SSoT](appointment-attribution-ssot.md) — admin all-employees overview & employee self-view must attribute appointments via the one shared rule (completed→performedBy, open→assigned else customer coverage); re-implementing per-view grouping drifts.
- [planCascade cascade SSoT](plan-cascade-ssot.md) — one pure pot-distributor; consume engine split into read+write phases; precompute==just-in-time because FIFO-avail is per-budgetType independent ⇒ byte-identical.
- [Test-data backlog & purge perf](test-data-backlog-purge-perf.md) — runTestDataCleanup is O(n) txns/customer; a big stale backlog hangs globalSetup & the test workflow → recover with a batched set-based purge (exact test-pattern filters); re-check audit_log rules after any killed purge.
- [Budget single Available reader + guard](budget-single-reader-arch.md) — ONE reader (readUnifiedBudgetAvailability); arch test guards dot-access `.netUsedInWindowCents` (consumers, not producer); known §45b manual_adjustment shadow drift is Phase-6's job.
- [Budget overview as-of-date used](budget-overview-asofdate-used.md) — overview totalUsedCents bounds transactionDate<=asOfDate in lockstep with allocated; breaks history===overview when tests book future-dated consumption ⇒ query overview ?date=latestApptDate.
- [§45b materialization double-count landmines](45b-materialization-landmines.md) — before persisting statutory_monthly rows: statistics/budgets.ts + /allocations listing blind-SUM/return ALL rows, consumption skip-set keys off initial_balance; switching read to SUM(rows) must be atomic across all of them.
- [Hard-hold gated engine testing](hard-holds-gated-testing.md) — BUDGET_HARD_HOLDS read in server process; HTTP/e2e can't reach gated branches → integration-test engine funcs DIRECTLY vs db; release-on-delete leans on orphan sweep (soft-delete, no FK cascade); `.replit` dev userenv leaks the flag into the ephemeral test server → orchestrator strips it.
- [Budget hard-block prod cutover](budget-hard-holds-prod-cutover.md) — BUDGET_HARD_HOLDS enabled in prod scope (keep gate, don't remove it); set via setEnvVars(environment:"production"); arch test guards against silent OFF-drift; takes effect only after re-publish.
- [Vitest4 config + harness-only flakes](vitest4-config-and-harness-flakes.md) — two projects w/ different maxWorkers need distinct sequence.groupOrder or full run aborts; some files flake ONLY under 2-worker harness (zugferd ZFP.1 pdf-hash, erstberatung), green isolated + in CI.
- [planHold §45b projectFuture horizon](budget-hold-projectfuture-horizon.md) — booking path must project §45b to the appt month (projectFuture) like the overview, not read it bare as-of the appt date, else future appts that fit the projection are falsely hard-blocked.
- [write_off classification allowlist trap](write-off-allowlist-trap.md) — moving budget code that pairs `'write_off'`+`transactionType` out of tests/ or server/scripts/ trips an arch test; add to ALLOWLIST + audit table (no-overdraw = allocation-view).
- [§45b deleted-IB anchor fallback](45b-deleted-ib-anchor.md) — calculateAllocated45b must anchor on soft-deleted initial_balance rows; else deleting the only IB on a customer w/o enabled type-settings hits the `!s45bEnabled` gate → renewal permanently €0 (skip-set/totals stay active-only).
- [§45b eligibility date-independent](45b-eligibility-date-independent.md) — calculateAllocated45b eligibility gate must check ALL §45b rows (`some(enabled)`), not today's; repro expired-window bugs via asOfDate not a frozen clock; billing/concurrency flakes = shared-DB contamination, not §45b regressions.
- [Billing split = pure consumption-row read](billing-split-depends-on-consumption-rows.md) — §45b "low budget" split tests fail because the consumption engine never books §45b→private overflow rows (§45a does); billing only reads what's booked, so it's a consumption-engine trait, not a billing-route bug.
- [Validation-env concurrency flakes](validation-env-concurrency-flakes.md) — agent harness runs test+e2e+app concurrently (CI runs them isolated) → PID-EAGAIN, count-races & timing flakes that are green in CI; verify suspect files via raw vitest against dev server, not the harness aggregate.
- [Full vitest run + project groupOrder](vitest-project-grouporder-blocker.md) — RESOLVED: unit=groupOrder 0, integration=1 (committed) so the full orchestrated `test` run no longer aborts at config; the check fires only AFTER globalSetup, so a bare timed-out `vitest run` proves nothing.
- [Storno date inheritance](storno-date-inheritance.md) — reversal rows MUST copy orig.transactionDate (not today) or asOfDate-windowed checks false-block; test-booking a PAST consumption needs all 4 cost params, §45b validFrom, sub-cap initial-budget.
- [Wizard §45b dual budget-write paths](wizard-budget-dual-write-paths.md) — create-payload `budgets` only writes type-settings+carryover (no initial_balance); §45b initial_balance comes ONLY from /initial-budget; 131€ start-month IB ≡ pure auto-renewal.
- [§45b prior-year initial-balance API guard](45b-prior-year-initial-balance-guard.md) — route rejects prior-year §45b Startwert (Übertrag); tests needing a prior-year §45b initial_balance must seed via upsertInitialBalanceAllocation (storage = preserved correction path), CRUD-only tests use current-year validFrom.
- [§45b carryover-shift horizon bound](45b-carryover-shift-horizon-bound.md) — the allocStart-shift (latestCountedCarryoverYear) and IB-floor must bound carryovers by validFrom<=asOfDate like carryoverTotal; a future-materialized carryover otherwise drags allocStart forward and zeroes §45b for backdated reads/bookings.
- [Phantom-Storno orphan reversals](phantom-storno-orphan-reversals.md) — import made NULL-link "Storno von Transaktion #N" rows that double-credit used; phantom iff ref linked-reversed OR ref-appt live; correct append-only (sign-flip all service cols), legit storno of cancelled appt skipped.
- [Phantom-pot invoice cleanup](phantom-pot-invoice-cleanup.md) — already-issued pot invoice whose pot is net-zero-reversed: clean up DOCUMENT-ONLY (negated stornorechnung + status), NEVER re-reverse by appointment (would credit back the live §45b sibling sharing that appt).
- [Invoice/LN VAT pot-bound & single-source](invoice-vat-pot-bound-single-source.md) — USt hängt am Budget-Topf (Kasse=exempt/§4Nr16, private=19%), nicht am billingType; Renderer verteilt persistierte USt-Summe (distributeVatAcrossLines), nie eigenständig ×1,19 ⇒ Σ(Brutto-Zeilen)===Gesamt; exempt unterdrückt Legacy-vatAmountCents-Krümel.
- [Fresh-DB test fragilities](fresh-db-test-fragilities.md) — fresh low-id ephemeral DB breaks tests matching cross-table raw ids (use unique business field), km-sum equality (use toBeCloseTo, not toBe), and bare GET /api/admin/customers/:id (doesn't exist; use /api/customers/:id or /api/admin/customers/:id/details).
- [Puppeteer footer vs body-footer](puppeteer-footer-vs-body-footer.md) — invoice/LN footers must be Puppeteer footerTemplate (inline styles + explicit font-size + empty headerTemplate + reserved bottom margin), never a body div (orphans a blank page); signature render-gate must accept same formats as sign-time.
- [Invoice+LN render test setup](invoice-ln-render-test-setup.md) — a pflegekasse_privat customer with NO funded pot reclassifies the invoice to selbstzahler ⇒ buildInvoicePdfBytes returns leistungsnachweisPdf=null; fund a Pflegekasse pot (§45b cap 13100) to get an LN; Beihilfe original=2×storno pages.
- [LN customerAddress binding](ln-customeraddress-binding.md) — Leistungsnachweis „Leistungsempfänger/in" must derive from customer MASTER (formatCustomerMasterAddress), not invoice.recipientAddress (=Kassen-Anschrift); LN-only render ⇒ GoBD invoice-hash verifier unaffected; draft UPDATE is trigger-allowed.
- [Net-zero re-bill re-books consumption](net-zero-rebill-rebook.md) — re-billing a fully-reversed appt re-books fresh cascade consumption at invoice GENERATE (not preview) so invoice==ledger; preview stays read-only; accepted preview/generate cascade drift.
- [PDF text extraction in route tests](pdf-text-extraction-in-tests.md) — to text-extract route-produced invoice/LN PDFs, normalize merged pdf-lib bytes before the old pdf-parse parser; LN shows the patient address, the Kasse only by name.
- [/send route needs SMTP config](send-route-stub-needs-smtp-config.md) — the billing send route validates SMTP config before the test-outbox stub, so send tests must seed dummy company SMTP settings first.
- [e2e currency-text assertions](e2e-currency-text-assertions.md) — formatEuroDE renders U+202F (narrow no-break space) before €, so `/.* € .*/` literal-space regexes never match; assert on data-testid guard elements instead.
- [Invoice PDF storage isolation](invoice-pdf-storage-isolation.md) — invoice/LN PDF object keys are env-scoped (prod=bare `invoices/…`, non-prod=`_nonprod/<NODE_ENV>/…`); new write paths MUST use buildInvoicePdfObjectKey + assertInvoicePdfWriteKeyAllowed, reads stay verbatim on stored path.
- [Invoice/LN PDF byte-instability](invoice-pdf-byte-instability.md) — PDFs carry wall-clock/XMP creation timestamps ⇒ a fresh re-render NEVER reproduces the sealed pdf_hash; any re-render-vs-pdf_hash gate flags ~everything (correct GoBD behavior); test repair paths by INJECTING the renderer; pre-#1047 legacy (no sealed pdfCreationDate) only repairable via hash-gated backup-byte restore, never snapshot backfill.
- [PDF determinism for byte-stable invoices](pdf-determinism-invoice-rerender.md) — freeze pdfCreationDate in render_snapshot; normalizePdfDeterminism length-preserving-patches CreationDate/ModDate/XMP/+/ID; tagged:false on Chromium; node-zugferd Info-dates frozen via embed metadata; merge already deterministic.
- [Invoice/LN page geometry SSoT & no HTML preview](document-page-geometry-ssot.md) — invoices/LN are PDF-only on screen (no HTML preview surface); margins+footer SSoT in shared/domain/document-page-geometry.ts, consumed by orchestrator+footer builders+DocumentPreview.
- [PDF @page margin override](pdf-page-margin-override.md) — `@page{margin}` in template CSS beats `page.pdf({margin})` ⇒ edge-to-edge; @page must be `size:A4` only, margins via Puppeteer; measure content box WITHOUT footer.
- [Bulk-print self-heal vs BP-3 trap](bulk-print-selfheal-bp3-trap.md) — persistInvoicePdf self-heals missing-object invoices ⇒ bogus-pdfPath "unrenderable" tests no longer fail the print loop; needs an invoice-targeted render-fault seam, not the global x-test-inject-fault.
- [Object storage shared bucket, keys now run+worker-scoped](object-storage-shared-bucket-collision.md) — non-prod PDF keys carry `/run-<EPHEMERAL_RUN_ID>/w-<EPHEMERAL_WORKER_ID>` (per-run alone clobbered across workers in one run minting same invoice nums); orchestrator clones worker DBs SERIALLY (concurrent clone ⇒ "being accessed by other users").
- [Weekend entry-type exemption is a 3-place change](weekend-entry-type-exemption.md) — allowing a time-entry type on weekends needs POST guard + PUT guard (server/routes/time-entries.ts, 2 isWeekend hits) + calendar DayCell visibility, else entries become uneditable/unreachable.
- [Invoice cumulative line render-gate](invoice-line-aggregation-render-gate.md) — Rechnung PDF+ZUGFeRD cumulate lines via ONE pure aggregateInvoiceLineItems (both layers); LN+persisted rows stay per-appointment; mode frozen per-invoice in renderSnapshot.lineAggregation (undef⇒per_appointment legacy byte-stable, new⇒cumulative).
- [PDF test introspection](pdf-test-introspection.md) — Chromium subsets fonts ⇒ content-stream regex sees glyph IDs not text; use pdf-parse(ToUnicode) for text, raw `re`-rect scan for geometry (needs zlib import); no backticks in pdf-generator CSS comments (kill the template literal).
- [ZUGFeRD EN16931 profile default](zugferd-en16931-profile-default.md) — new invoices embed EN16931; sealed invoices (snapshot.profile undefined) MUST re-render as BASIC for GoBD hash stability; non-strict fallback is audit-logged not silent; validation gate skips without Java.
- [EN16931 backfill decision](en16931-backfill-decision.md) — old BASIC invoices are NOT force-re-sealed to en16931 (BASIC already EN-16931-conformant + GoBD-sealed); contingency = storno+reissue, not in-place re-seal.
- [Kasse "missing pot" invariant trigger](invoice-missing-pot-invariant.md) — generateInvoiceCore pot-unresolved 400 only fires on EMPTY line items (kasse no-show = no line item); single private pot reclassifies to selbstzahler, §45a-only is correctly stamped not blocked.
- [audit_log INSERT schema trap](audit-log-insert-schema-trap.md) — real cols are metadata(jsonb)+user_id NOT NULL FK; the old startup-migration `changes`/user_id=NULL copy-paste INSERT silently no-ops; resolve a system actor + use metadata.
- [Customer-detail route split](customer-detail-route-split.md) — two distinct detail pages: §45b FIFO bar only on /customer/:id (pages/customer-detail.tsx), NOT /admin/customers/:id (pages/admin/customer-detail.tsx).
- [§45b/§45a/§39 anchor = runtime per pot](45b-anchor-origin-stamp.md) — Task #1204: NO persisted budget_start_date(_origin)/'manual'; anchor derived at runtime from Pflegegrad history (earliestCareLevelStart) — §45a/§39 raw uncapped, §45b floored to current-year Jan1; no single shared anchor.
- [Customer status = "aktiv" + intake checklist](customer-status-aktiv-value.md) — active-customer filters/counts use German "aktiv" not "active" (latent bug); in-intake = aktiv & no contract; SSoT computeIntakeChecklist in shared/domain.
- [Guarded migration conservation needs materialized alloc](guarded-migration-conservation-needs-materialized-alloc.md) — runGuardedBudgetMigration's no-overdraw guard counts only materialized budget_allocations; §45b books via projection ⇒ fund within statutory cap + assert initial-budget 200, else rollback.
- [Type-settings new-version-ab-heute vs BUG-13](budget-typesettings-new-version-today.md) — PUT ohne validFrom auf Zukunfts-Zeile MIT in-Kraft-Vorgänger = neuer Versionssatz ab heute (Phasen-Append, kein Pull-Forward); ohne Vorgänger bleibt BUG-13 Pull-Forward.
- [esbuild straight-quote = 0-test file](esbuild-straight-quote-zero-tests.md) — a stray straight `"` inside a `"`-delimited it()/describe() title makes the whole vitest file collect "(0 test)" via an esbuild transform error; tsc can stay green; trust synchronous `vitest run` not typecheck.
- [Budget default-pots resolver](budget-default-pots-resolver.md) — effectiveDefaultPots(customer) is the eligibility-gated SSoT for default pots; raw DEFAULT_BUDGET_POT_ORDER is module-private (eslint + arch test guard); §45b default-on only for non-Selbstzahler.
- [budget_* side tables (ledger removed)](budget-three-tables-not-removable.md) — budget_ledger REMOVED via staged A→B→C (mirror+captured_ledger_id dropped, append-only now on budget_transactions); _reservations (live holds) + _migrations (journal) PERMANENT; budgetLedgerStorage facade ≠ the dropped table.
- [SQL NOT on nullable predicate drops rows](sql-not-nullable-predicate-drops-rows.md) — a `NOT (col = 'x' OR …)` filter silently drops rows where the col is NULL (3-valued logic ⇒ NULL ⇒ WHERE fails); wrap the inner predicate in COALESCE(…, false). Hit it in the customer laufend/gekündigt lifecycle filter (no-contract customers vanished from "laufend").
- [Doc-only import vs billing split](import-doc-only-billing-landmine.md) — prior-year "documentation-only" imports create completed appts with ZERO consumption rows; invoice per-pot split reads zero-rows as private/Selbstzahler ⇒ never bill prior-year months with these, and "no rows == self-payer" is a false assumption.
- [Private-pot eligibility gate](private-payment-pot-gate.md) — one SSoT isPrivatePaymentAllowed; private 19% pot/invoice gated in consumption/reservation/import/net-zero-rebook write paths + invoice-calc split backstop; net-zero rebook was the silent leak; keep budget=0 single private invoice.
- [Selbstzahler statutory-pot defense-in-depth](selbstzahler-statutory-pot-defense-in-depth.md) — upsertBudgetTypeSettings now THROWS for selbstzahler+enabled statutory pot (not only the route); deactivate always allowed; legacy/correction writers must pass {allowStatutoryForSelbstzahler:true}.
- [Prior-year Pflegekasse import = documentation-only](import-prior-year-documentation.md) — backdated non-private-payer import books appt+services but NO budget (year floored ⇒ 0 avail); SSoT isDocumentationOnlyImport (shared/domain/import-documentation-only.ts), recomputed server-side via loadPrivatePaymentAllowed; test traps: createTestCustomer defaults acceptsPrivatePayment:true, importer errors on weekend dates.
- [CI gaps: no object-storage sidecar + reference seed](ci-object-storage-and-reference-seed.md) — GitHub-Actions CI has no obj-storage sidecar (skip invoice/PDF tests via tests/helpers/object-storage.ts) and no base services/company_settings (run seed-test-reference-data.ts in CI; nullable updated_by_user_id must be null not 0).
- [One-time delete needs seeder ledger-gate](one-time-delete-seeder-ledger-gate.md) — exactly-once cleanup of SEEDED rows resurrects next boot unless seeders also gate inserts on the migration-ledger flag (which must tolerate a missing ledger table; seeders run before ensureMigrationLedger).
- [Budget-Ledger dual-link (Stufe A→C)](budget-ledger-dual-link.md) — captured_transaction_id ist seit Stufe C der EINE Capture-Link; captured_ledger_id + budget_ledger-Spiegel ENTFERNT; ConservationResult.linkDivergences (0=gesund).
- [Company-settings cross-process cache trap](company-settings-cross-process-cache-trap.md) — seed via orchestrator app-server + read via in-process router = 2 storage-tier caches; invalidate BOTH storage.invalidateCompanySettingsCache() AND companySettingsCache.invalidate(), or /send reads stale creds.
- [Budget type-settings one SSoT, many callers](budget-typesettings-two-write-paths.md) — ONE SSoT write path; PUT type-settings + POST customers create + POST /initial-budget all funnel through it sharing the validators (no 2nd layer); /initial-budget only ENABLES (no limits → no §45a/§39 max check); parity needs validFrom + current-year dates.
- [Pot-only rebook needs force](rebook-pot-change-needs-force.md) — moving existing consumption to a different budget pot with unchanged km/minutes/date is a silent no-op unless rebookAppointmentConsumption is called with force:true.
- [Service catalog config-driven SSoT](service-catalog-config-driven.md) — services only via shared/config/services.ts; POST/PUT→403; startup sync THROWS on DB drift but it's swallowed by fault-isolation, so the real "no junk" guarantee is getAllServices() filtering to SERVICE_CATALOG_CODES (byId/byCode stay unfiltered for FK safety); no service seeding in tests.
- [priceFor pricing SSoT](pricefor-ssot.md) — ONE price resolver (override→standard→catalog-default); customer-row EXISTENCE wins even at cents=0 (`??` never `||`); by-ID lookup stays FK-safe unfiltered.
- [Rate-literal magic-number guard](rate-literal-arch-guard.md) — prices/wages/km-rates SSoT-only; catalog cents (3800/1600/4200/1800) live ONLY in services.ts + recover-prices; arch test flags hardcoded rates gated on money keyword, escape `// rate-literal-allowed:`.
- [Price consolidation origin provenance](price-consolidation-origin-provenance.md) — additive unified `prices` table: `scope` alone collides customer-service-prices with customer-contract-rates; every per-source reader/writer + populate natural key MUST also filter/include `origin`.
- [Junk master-data purge (BUG-18)](junk-master-data-purge-bug18.md) — doc-type junk = `DOC`-prefix NOT in DOCUMENT_TYPE_WHITELIST (real types never start DOC; add new DOC-named types to whitelist); purge FK-safe (deactivate referenced, hard-delete referenceless); guard counts only ACTIVE junk; prod migration #895-pattern, approval-gated.
- [Conservation projection-aware](conservation-projection-aware.md) — no-overdraw (I13) + FIFO-equality enumerate (consumption ∪ entitlement) & evaluate overdraw via readUnifiedBudgetAvailability (projection), NOT raw budget_allocations; write_off classification lives only in the reader (file dropped from write-off allowlist).
- [CI background-PDF pool stampede](ci-background-pdf-pool-stampede.md) — no-sidecar CI: transaction-wrapped Puppeteer persist + 3×30s retries drains the DB pool ⇒ unrelated reqs 401/500 via connect-timeout; gate persist on isObjectStorageConfigured(); auth swallows DB timeout as 401.
- [Prod big-bang publish of budget_ledger drop](prod-bigbang-publish-budget-ledger.md) — far-behind prod gets a staged DDL migration all at once via Replit Publish; safe iff dropped table is redundant in the surviving SoT + no startup backfill strands a column + consumers tolerate NULL; answer rename prompt "No, create new table".
- [§45b Juli-Boundary symmetry](45b-july-boundary-symmetry.md) — Allocated dropping an expired carryover / superseded prior-year IB MUST also drop that pot's H1 consumption from ConsumedNet (overview AND booking), else current-year pot double-charged → false July hard-block.
- [§45b forecast signed-available SSoT](45b-forecast-signed-available-ssot.md) — forecast loops read raw `allocatedCents − consumedNetCents` from netAvailable45bAt({projectFuture,holds:"ignore"}), NOT floored availableCents; #1340 carryover-exclusion lives ONLY in the SSoT (arch-guarded against re-hand-rolling getExcluded45bConsumption in summary-queries).
- [§45b carryover-Verfall no-chaining + IB decoupled](45b-carryover-verfall-no-chaining.md) — Y leftover expires own 30.06(Y+1), never rolls to Y+2 (auto-roll subtracts totalCarryoverIn first); IB floor = expiryFloorAnchorYear NOT latestCarryoverYear, supersede only {validCarryover.year-1}.
- [Render outside DB transaction](render-outside-transaction.md) — PDF render must never run inside db.transaction/withAudit (pool starvation); ast-grep fitness test guards billing routes + orchestrator; onboarding-in-tx is the accepted exception.
- [Additive-only publish diff strategy](additive-publish-diff-strategy.md) — Replit publish auto-diff can emit a redundant DROP for an already-cascade-removed object & abort; keep each publish additive (restore legacy objects in dev), defer removal, verify dev vs read-only prod replica (tables/cols/idx/constraints/enums/defaults).
- [Startup imports fire script main()](startup-import-fires-script-main.md) — importing a server/scripts/* export into a startup/runtime path runs its unguarded top-level main()+process.exit at boot, killing the server (typecheck/lint won't catch it); guard every script entrypoint with `if (import.meta.url===file://argv[1])`.
- [Publish image >8 GiB](chromium-userdatadir-image-bloat.md) — whole workspace packs into the Repl layer; dev cruft (.config/chromium 5GiB, .local/.cache/.git/tmp) blows the 8 GiB limit; durable fix = .replitignore (NOT .gitignore) + Puppeteer userDataDir to /tmp; read real cause via getDeploymentBuild.
- [Workspace bloat = orphaned test artifacts](workspace-bloat-test-artifacts.md) — live-workspace freezing (≠ publish image) = crashed runs stranding .local/test-server-<runId>.cjs + test-client-<runId>/; auto-sweep guards: runId-liveness (bundle mtime is stale), always-on 60s force-floor, process-sweep-before-artifact-sweep.
- [Invoice-PDF pool-stress test](invoice-pdf-pool-stress-test.md) — stress test must STAGGER the surge (all-at-once self-deadlocks Phase-1 nested-acquire, a different bug); Phase-2-in-held-tx regression detected via foreign-read latency/waiting AND maxActive<POOL_MAX backstop; harness windows uncatchable.
- [prices partial unique index](prices-partial-unique-index.md) — unified `prices` needs a deleted_at-partial unique idx keyed by scope+origin+customer+service+valid_from (lost with the cutover file); writers must soft-delete-before-insert.
- [billing coverage-gate floor & hard-holds leak](coverage-gate-billing-floor.md) — the gate spawns its own dev server that must strip BUDGET_HARD_HOLDS (else low-budget fixtures hard-block at appointment-creation → false timeout); honest single-file coverage (~25%/44%) is low because bulk routes are tested in sibling files & the gate skips in CI w/o object storage → recalibrating the floor down is honest, not masking.
- [SSoT boot-gate ordering](ssot-boot-gate-ordering.md) — a boot gate that aborts on an empty critical SSoT MUST run AFTER its fill/recovery migrations (both pre-listen), else the gate exits before its own fix runs → publish boot deadlock.
- [Statistics economics (Wirtschaftlichkeit) SSoT](statistics-economics-ssot.md) — non-billable valued at HW rate, time-entry km at travel_km rate, rates only from catalog; ONE marginPercent reused by revenue+performance; stats tests assert specific props so additive response fields are safe.
- [Publish safety net boot gate](publish-safety-boot-gate.md) — critical-SSoT boot gate THROWS only in prod (dev/test legitimately run empty prices ⇒ warn-only or every workflow boot breaks); destructive publish drops detected via prod-replica schema diff (not migration grep) + per-DROP PUBLISH_ACK_DROPS + expand-migrate-contract; build warning stays non-blocking.
- [Prod read-only investigation access](prod-readonly-investigation-access.md) — managed prod executeSql breaks on rotated creds; read via PROD_DATABASE_URL+pg (READ ONLY, never print); app db.ts pool hangs vs prod from dev; GoBD writes (storno PDF) are env-scoped ⇒ prepare script in dev, --apply only in prod.
- [mark_task_complete platform-step failures](mark-task-complete-infra-steps.md) — rebase/checkpoint run BEFORE validation; error + validation_run_result:None = platform infra (not code); don't loop retries or run git manually; if checkpoint timeout repeats after .local cleanup, size isn't it — report blocker.
- [Replit runButton & Project boot composite](replit-runbutton-boot-composite.md) — agent workflow tools cant exclude a workflow from the parallel Project boot or set runButton; only a manual run-button flip commits the fix; boot-path arch test red in agent env until then.
- [Cockpit drill lazy pagination](cockpit-drill-lazy-pagination.md) — cockpit overview returns ONLY SQL aggregates; per-stage rows come from a separate paginated GET /billing/cockpit/drill loaded on stage-click — never re-inline rows (1.3MB→infinite spinner).
- [Billing pipeline read-model](billing-pipeline-readmodel.md) — Abrechnung board SSoT composes the revenue-stats formula; revenue `planned` filters status IN (scheduled,completed,documented) where `documented`≠real status ⇒ `documenting` silently excluded; cross-check €-conservation per-customer on scheduled-only fixtures.
- [billing-flow Selbstzahler-create flake](billing-flow-szinvoice-create-flake.md) — agent-env 6-workflow restart-loop flakes invoice creation (PDF/obj-storage/pool); BF-9.1 canary cascades to every createDraftSzInvoice test (BF-10.3/4/7/9); green same-route sibling (BF-10.8) + red BF-9.1 == flake, verify in isolated CI not the harness loop.
- [Billing review-cluster reuses breakdown SSoT](billing-review-cluster-ssot.md) — Phase-2 /review-clusters must group AND total via groupBillingBreakdown+summarizeBreakdownRows (no hand-rolled math); "Kasse" dimension = billingType (like Phase-1), NOT individual insurer; server returns both dimension clusters pre-aggregated so the toggle needs no refetch; generation still loops per-customer.
- [Ephemeral-test PID exhaustion sweep](ephemeral-test-pid-exhaustion.md) — orphaned Chromium+test-app-server from hard-killed runs eat cgroup pids.max; sweep extended with process dim + PID-preflight, workers spawned detached(own pgroup) for group-kill + exit-reaper, advisory-lock worker-slot gate; all fail-safe; manual `npm run test:unblock`.
- [Chromium cold-start stampede](chromium-coldstart-stampede.md) — prod PDF 60s launch timeouts = Autoscale instances cold-reading /nix/store Chromium simultaneously; de-stagger via boot jitter + DB advisory-lock + page-cache warm + bounded prewarm retry (never lower 60s timeout, never touch render/pdf_hash); network-service-crash log = symptom; Reserved VM = structural root-fix (out of scope).
- [Sammel-LN on-demand model](monthly-ln-pending-merge.md) — LN create is on-demand & NON-merging (each call = new separate Sammel-LN, appointmentIds-selectable); coverage-exclusion is app-level made race-safe by tx FOR UPDATE lock on the appt rows + re-check (no DB unique; plain unique on appointment_id is WRONG). Sealed-never-mutated, overview monthlyRecords[]. Loser of concurrent create = 400 or 409.
- [Records-without-invoice appt-precise](records-without-invoice-appt-precise.md) — process-health "noch abzurechnen" must check per-appointment (mirror getAlreadyInvoicedAppointmentIds SSoT: line on non-storniert/non-stornorechnung invoice), NOT period-coarse "any invoice for customer+year+month", else sibling/partial-month proofs vanish.
- [Reporting rate labels effective](reporting-rate-labels-effective.md) — the 3 reporting readers must derive per-unit rate LABELS from money÷quantity (effective wages/prices), not catalog cols; qty≤0⇒0; drift-test MUST book the REAL hauswirtschaft service (readers filter by code).
- [Payroll "Noch nicht abrechenbar" count↔list join](mitarbeiterabrechnung-unsigned-customer-join.md) — overview counter has NO customer join; detail list/drill-down must LEFT JOIN customers (+placeholder), never INNER, or prospect/Erstberatung appts (customer_id NULL) count but vanish from the list.
- [No-show km edit path](no-show-km-edit-path.md) — existing no-show km is corrected via reopen→re-document (POST /document-no-show, writes noShowKilometers SSoT); generic PATCH /appointments/:id is 403-blocked for customer_no_show (canModifyAppointment false) & edit form has no km field.
- [km-display arch-test allowlist](km-display-allowlist-arch-guard.md) — any client page rendering `${...} km` aggregates (not invoice lines) must be in ALLOWED_PATHS of tests/architecture/km-display-via-helper.test.ts; on Ersetzungs-Regel deletion, swap the entry to the successor page, don't just drop it.
- [Document upload version-vs-append](document-upload-batch-append.md) — multi-page docs = rows sharing batchId; append reuses batchId + skipDeactivation:true and MUST carry the batch's label/date (grouping picks newest-row-per-batch); one batchId per multi-file upload.
- [Qonto billing-irrelevant flag](qonto-billing-irrelevant-flag.md) — soft-flag mutually exclusive with matched: guard isNull in ALL match UPDATEs not just read-time; credit-only; survives re-sync (upsert omits it).
- [Qonto test global.fetch stub leak](qonto-test-fetch-stub-leak.md) — request-level apiPost/apiDelete suites placed after fetch-stubbing Qonto suites fail login with "CSRF-Token nicht in Cookies gefunden"; add vi.unstubAllGlobals() in their own beforeAll.
- [Mitarbeiterabrechnung HW/AB + absence blocks](mitarbeiterabrechnung-hw-ab-absence.md) — overview recorded-hours = 2 Lexware pots (HW=work+nebenzeiten, AB=Alltagsbegleitung), Urlaub/Krankheit/Feiertage EXCLUDED from HW; absence Von–Bis via reconstructAbsenceBlocks (Σ tage === tageUrlaub/tageKrankheit, same date/holiday helpers as expansion).
- [edit-persistence invoice tests need object-storage guard](edit-persistence-invoice-objectstorage-guard.md) — the km/duration drift tests fail in no-sidecar CI at `invoiceRow` visible (needs obj-storage/PDF like billing-bulk); gate the invoice block on hasObjectStorage; NOT a formatting drift or harness flake.
- [Budget type-settings backdate guard](budget-typesettings-backdate-guard.md) — „Gültig ab" backdate reject is freshness-independent: only non-fresh valued rows in-window block; fresh edited row excluded as own blocker; don't re-gate on current/currentIsFresh.
- [Import billed/signed protection SSoT](import-billed-signed-protection-ssot.md) — one getMutationProtectedAppointmentIds reader → {protectedIds,signedServiceRecordIds,invoicedIds}; Excel import uses protectedIds union, reconcile ONLY signedServiceRecordIds (wiring invoiced in = behavior change).
