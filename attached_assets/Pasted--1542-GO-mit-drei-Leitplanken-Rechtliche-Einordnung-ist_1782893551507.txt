#1542 — GO mit drei Leitplanken. Rechtliche Einordnung ist bestätigt (Einzel- und Sammelnachweis sind beide zulässig), das Konzept passt.
1. Koordination mit #1526 — nicht doppelt bauen. #1542 ersetzt den Auto-Monats-LN und macht damit den Pending-Merge-Pfad aus #1526 gegenstandslos (kein Auto-Container mehr, in den gemergt wird → auch der record_type='monthly'-Pending-Unique-Index entfällt, wie in Step 1 vorgesehen). Was aus #1526 bleiben muss: (a) die Overview-Sichtbarkeit (Overwrite-Bug bei monthlyRecordMap.set(customerId,…) in service-records-storage.ts — ein Kunde kann weiter mehrere bewusste Bündel + Einzel-LN haben, alle müssen sichtbar sein) und (b) die Remediation der Alt-Duplikate. Bitte den Pending-Merge NICHT bauen und dann wieder rausreißen — direkt auf das #1542-Modell gehen und aus #1526 nur Sichtbarkeit + Remediation übernehmen.
2. Sichtbarkeits-Sicherheitsnetz — Pflicht. Der Wegfall des Auto-Monats-Containers entfernt die automatische Sammlung, die heute verhindert, dass Termine vergessen werden. Ersatz muss die Übersicht leisten: „dokumentierte, aber noch in keinem signierten LN enthaltene Termine" je Kunde+Mitarbeiter zuverlässig surfacen (an die #1496-„fehlende Unterschriften"/„Bereit"-Liste andocken). Sonst tauschen wir Duplikate gegen vergessene Termine. Bitte diese Sicht als Teil von Step 5 explizit absichern.
3. record_type='monthly'-Annahmen vollständig auditieren (Step 6 bestätigt) — Bündeldruck /billing/:id/bundle, Billing-Reader, „noch abzurechnen"-Health, Filter, Statistik. Keine Pfade dürfen brechen, wenn monthly zu Sammel-LN wird. Die Abrechnung ist termin-getrieben & storno-sicher (getAlreadyInvoicedAppointmentIds, Kandidatenpool über ALLE signierten Proofs) — der LN-Typ ist nur Doku-Verpackung, das Billing bleibt unangetastet.
GoBD/Migration: Bestehende signierte monthly-LN nie mutieren (nur Semantik-Umdeutung zu Sammel-LN, keine Daten löschen), Migration idempotent im Startup-Pfad, kein drizzle-kit push auf Bestandsspalten. Die Daten-Migration (monthly→Sammel) mit der #1526-Remediation der Alt-Pending-Duplikate koordiniert fahren, und vor jedem Prod-Write read-only-Report + Sign-off (dev dry-run, prod erst nach Freigabe mit --apply).
Coverage-Ausschluss (ein Termin nur in einem LN) und Sealed-never-mutated bleiben Invarianten. Ansonsten: build-ready.