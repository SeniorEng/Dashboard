KORREKTUR #1325 — Preis-Cutover: main (5854bfa) ist so NICHT veröffentlichbar. Erst dieser Fix, dann Publish.
Problem: price-for.ts und die service-prices-Route lesen jetzt unbedingt aus prices, aber nichts befüllt prices in Prod (populate-Skript ist dev-only; keine SQL-/Startup-Migration). Ein Publish würde aus einer in Prod leeren/unvollständigen prices-Tabelle lesen → Kundenpreise/Overrides fehlen → falsche Abrechnung. Prod läuft aktuell noch sicher auf bcb903b (Alt-Tabellen). Nicht veröffentlichen, bis der Fix drin ist.
Bitte bauen (kein Publish, am Ende pushen):

Idempotente Startup-Migration, die prices in ALLEN Umgebungen (inkl. Prod) aus den drei Alt-Tabellen befüllt — additiv, also nicht flag-gegated (das Befüllen ist ungefährlich und für Korrektheit nötig). Läuft beim Boot vor dem Serving, einmalig über den Migrations-Ledger. Logik wie populate-prices.ts: verlustfrei, origin-gestempelt, dedupliziert über Natural Key inkl. origin, Zeilen ohne validFrom reporten statt verwerfen. Wichtig: Die Alt-Tabellen sind aus dem Drizzle-Schema raus, physisch aber noch da → per raw SQL lesen (SELECT … FROM customer_service_prices / customer_contract_rates / service_rates).
Gate-2-Paritäts-Selbstcheck direkt nach der Befüllung: konsolidierte priceFor-Auflösung == unabhängige Legacy-Auflösung über die volle Abdeckung (Termin-Tupel + alle ccr-Paare + alle service_rates-Services). Bei irgendeiner Abweichung ≠ 0 Cent → Startup hart fehlschlagen (nicht mit falschen Preisen serven) und den konkreten Diff reporten. Kein stilles Weiterlaufen.
Pre-Publish-Verifikation (im Migrations-Log ausgeben): prices-Zeilenzahl in Prod == Summe der drei Alt-Tabellen je origin; Stichprobe: der bekannte 0-Override-Kunde („keine Anfahrt") löst auf 0 auf.
Physischer DROP der drei Alt-Tabellen bleibt separat als flag-gegated Migration (#1326) — erst nachdem Prod nachweislich sauber auf prices läuft.
Tests grün halten (priceFor-Unit, 0-vs-NULL, Gate-2, Conservation-Fixtures, BUG-17 rot). Danach committen + pushen, kein Publish, kein Flag setzen.