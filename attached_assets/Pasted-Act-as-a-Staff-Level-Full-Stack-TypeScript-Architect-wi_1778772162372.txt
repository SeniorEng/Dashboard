Act as a Staff-Level Full-Stack TypeScript Architect with deep expertise in financial systems, Clean Architecture, and German GoBD compliance.

I am providing you with the core files of our "Budget System" module.

Our strict Tech Stack: React 19, TypeScript, TanStack Query, Express, Zod, Drizzle ORM, PostgreSQL (Neon).

The Budget System is highly critical. It must perfectly track financial limits, consumptions, and mutations without losing synchronization between the database and the frontend.

Please conduct a rigorous static analysis of the provided code. Focus exclusively on architecture, logic flaws, and financial anti-patterns. Do not rewrite everything—provide a diagnostic report covering the following areas:

1. Financial Integrity & Aggregation (Drizzle / Postgres)

How are financial values (currency/cents) handled? Point out any risks of floating-point math errors.

Are budget consumptions calculated in memory (Node.js/Express) instead of utilizing efficient PostgreSQL aggregations via Drizzle?

Identify any N+1 query problems in the budget calculation.

2. GoBD Compliance & Mutation Handling

Budgets change over time. Analyze the update/delete logic. Are we accidentally violating GoBD by hard-deleting records or overwriting values without creating an immutable audit trail / history entry?

3. Caching & State Synchronization (TanStack Query)

Analyze the React custom hooks and TanStack Query setup.

Is the invalidateRelated() logic correctly ensuring that when a transaction occurs, the related budget queries are immediately invalidated? Point out race conditions or over-fetching.

4. Business Logic Leaks

Is the budget calculation logic duplicated across the backend controllers and frontend components?

Should some of this logic be encapsulated in a shared pure function or a strict backend domain service?

Output format:
Create a structured markdown report. For every issue found, provide the exact file name/line, explain the financial or architectural risk, and provide a strict, step-by-step refactoring instruction to solve it.